version: 0.2

phases:
  install:
     runtime-version:
     nodejs: 18
    commands:
      - npm install -g htmlhint

  build:
    commands:
      - echo "Validating HTML..."
      - htmlhint "**/*.html"

artifacts:
  files:
    - '**/*'
